@model List<Vistas>


@foreach (var item in Model)
{
    <div class="text-center">
        <a class="nav-link text-dark" asp-area="" asp-controller="@item.Controlador" asp-action="@item.Accion">@item.nombrevista</a>
    </div>

}
